# Data Folder

Place user-provided CFA portfolio-related PDFs here. These will be processed by the deployment scripts.

**Note**: Large PDFs and processed JSON files are excluded from git (see `.gitignore`). They should be:
1. Downloaded locally
2. If using Hugging Face Spaces, uploaded via the Space's file browser interface
